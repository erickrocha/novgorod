import { useEffect, useMemo, useState } from "react";
import { Pencil, RefreshCw, Upload } from "lucide-react";
import PageBreadcrumb from "@/components/common/PageBreadCrumb";
import PageMeta from "@/components/common/PageMeta";
import ComponentCard from "@/components/common/ComponentCard";
import Button from "@/components/ui/button/Button";
import Input from "@/components/form/input/InputField";
import Label from "@/components/form/Label";
import { useAppDispatch, useAppSelector } from "@/store/hooks";
import { fetchCities, fetchProvinces, importCities, importProvinces, saveCity, saveProvince } from "@/store/locationSlice";
import type { City, Province } from "@/services/types";

export default function Locations({ kind }: { kind: "provinces" | "cities" }) {
  const dispatch = useAppDispatch(); const state = useAppSelector(s => s.location); const [query, setQuery] = useState("");
  const [editing, setEditing] = useState<Province | City | null>(null);
  useEffect(() => { dispatch(fetchProvinces()); dispatch(fetchCities()); }, [dispatch]);
  const provinces = state.provinces; const rows = kind === "provinces" ? state.provinces : state.cities;
  const filtered = useMemo(() => rows.filter(r => r.name.toLowerCase().includes(query.toLowerCase())), [rows, query]);
  const submit = (event: React.FormEvent<HTMLFormElement>) => { event.preventDefault(); const form = new FormData(event.currentTarget); if (kind === "provinces") dispatch(saveProvince({ ...(editing as Province || {}), ibgeCode: String(form.get("ibgeCode")), acronym: String(form.get("acronym")), name: String(form.get("name")), countryCode: "BR" })); else dispatch(saveCity({ ...(editing as City || {}), ibgeCode: String(form.get("ibgeCode")), provinceId: Number(form.get("provinceId")), name: String(form.get("name")) })); setEditing(null); };
  const upload = (event: React.ChangeEvent<HTMLInputElement>) => { const file = event.target.files?.[0]; if (file) dispatch(kind === "provinces" ? importProvinces(file) : importCities(file)); event.target.value = ""; };
  return <><PageMeta title={`${kind === "provinces" ? "Provinces" : "Cities"} | Veche`} description="Manage Brazilian locations"/><PageBreadcrumb pageTitle={kind === "provinces" ? "Provinces" : "Cities"}/><ComponentCard title={`Manage ${kind}`}><div className="mb-5 flex flex-wrap items-end justify-between gap-3"><div><Label htmlFor="location-search">Search</Label><Input id="location-search" value={query} onChange={e => setQuery(e.target.value)} placeholder="Search by name"/></div><div className="flex gap-2"><Button variant="outline" startIcon={<RefreshCw size={16}/>} onClick={() => { dispatch(fetchProvinces()); dispatch(fetchCities()); }}>Refresh</Button><label className="inline-flex cursor-pointer items-center rounded-lg bg-brand-500 px-4 py-3 text-sm font-medium text-white"><Upload size={16} className="me-2"/>Import CSV<input type="file" accept=".csv,text/csv" className="hidden" onChange={upload}/></label><Button onClick={() => setEditing(kind === "provinces" ? { acronym: "", name: "", countryCode: "BR", ibgeCode: "" } : { provinceId: provinces[0]?.id || 0, name: "", ibgeCode: "" })}>Add</Button></div></div>{editing && <form onSubmit={submit} className="mb-6 grid gap-4 rounded-lg border border-gray-200 p-4 md:grid-cols-4"><div><Label htmlFor="ibgeCode">IBGE code</Label><Input id="ibgeCode" name="ibgeCode" defaultValue={editing.ibgeCode || ""} required/></div>{kind === "provinces" && <div><Label htmlFor="acronym">Acronym</Label><Input id="acronym" name="acronym" defaultValue={(editing as Province).acronym} required/></div>}{kind === "cities" && <div><Label htmlFor="provinceId">Province</Label><select id="provinceId" name="provinceId" defaultValue={(editing as City).provinceId} className="h-11 w-full rounded-lg border border-gray-300 px-3 text-sm">{provinces.map(p => <option key={p.id} value={p.id}>{p.name}</option>)}</select></div>}<div><Label htmlFor="name">Name</Label><Input id="name" name="name" defaultValue={editing.name} required/></div><div className="flex items-end gap-2"><Button type="submit">Save</Button><Button type="button" variant="outline" onClick={() => setEditing(null)}>Cancel</Button></div></form>}{state.error && <div className="mb-4 rounded-lg border border-error-200 bg-error-50 p-3 text-sm text-error-600">{state.error}</div>}{state.report && <div className="mb-4 rounded-lg border border-success-200 bg-success-50 p-3 text-sm">Imported: {state.report.inserted} inserted, {state.report.updated} updated, {state.report.skipped} skipped.</div>}<div className="overflow-x-auto"><table className="w-full text-start text-sm"><thead><tr className="border-b border-gray-100"><th className="px-4 py-3">IBGE code</th><th className="px-4 py-3">Name</th>{kind === "provinces" && <th className="px-4 py-3">UF</th>}<th/></tr></thead><tbody>{filtered.map(row => <tr key={row.id || row.ibgeCode} className="border-b border-gray-100"><td className="px-4 py-3">{row.ibgeCode || "—"}</td><td className="px-4 py-3 font-medium">{row.name}</td>{kind === "provinces" && <td className="px-4 py-3">{(row as Province).acronym}</td>}<td className="px-4 py-3 text-end"><Button variant="outline" onClick={() => setEditing(row)} startIcon={<Pencil size={15}/>}>Edit</Button></td></tr>)}</tbody></table></div></ComponentCard></>;
}
